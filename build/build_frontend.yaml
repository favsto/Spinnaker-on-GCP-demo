steps:

# Build image
- name: 'gcr.io/cloud-builders/docker'
  args:
  - 'build'
  - '--tag=eu.gcr.io/$PROJECT_ID/micro-frontend:$TAG_NAME'
  - '--file=source/frontend/Dockerfile'
  - './source/frontend/'

# release image
images:
- 'eu.gcr.io/$PROJECT_ID/micro-frontend:$TAG_NAME'